node_modules/
coverage/

# Vite dev/demo build output (npm run build) — a different folder than the
# release scripts below on purpose, see scripts/build-release.js's header.
dist/

# Release artifacts (scripts/build-release.js, package-release.js,
# build-tests-release.js, package-tests-release.js)
dist-release/
dist-tests/
toolcrib.zip
toolcrib.zip.sha256
toolcrib-tests.zip
toolcrib-tests.zip.sha256

# Local scratch data from cli/CONTRIBUTING.md's integration-test instructions
cli/integration-test/releases/

# Playwright (npm run test:e2e) output — see e2e/README.md
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Curated visual reference (npm run generate-storyboard) — regenerated on
# demand, not a pixel-diff regression baseline, not checked in.
/storyboard/

# temp folder for in-flight plans from other AI sessions
.plans/

# Claude Code project settings accumulate session-local entries (e.g.
# additionalDirectories pointing at another local project's absolute
# path) as a normal part of how the tool manages permissions during a
# session -- not safely shareable across contributors/machines. Untracked
# going forward (was previously committed; removed via `git rm --cached`).
.claude/settings.json
